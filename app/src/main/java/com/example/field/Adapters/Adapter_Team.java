package com.example.field.Adapters;import android.app.AlertDialog;import android.content.Context;import android.content.DialogInterface;import android.content.Intent;import android.view.LayoutInflater;import android.view.MenuItem;import android.view.View;import android.view.ViewGroup;import android.widget.ImageView;import android.widget.PopupMenu;import android.widget.TextView;import android.widget.Toast;import androidx.annotation.NonNull;import androidx.recyclerview.widget.RecyclerView;import com.example.field.Navlayout;import com.example.field.News_Recycler;import com.example.field.R;import com.example.field.Uni;import com.example.field.friday_cardview;import java.util.ArrayList;import java.util.Arrays;import java.util.List;public class Adapter_Team extends RecyclerView.Adapter<Adapter_Team.ViewHolder> {    Context mcontext;    ArrayList<teamlist> mode;    public Adapter_Team(Context mcontext, ArrayList<teamlist> mode) {        this.mcontext = mcontext;        this.mode = mode;    }    @NonNull    @Override    public ViewHolder onCreateViewHolder(@NonNull ViewGroup parent, int i) {        View v = LayoutInflater.from(parent.getContext()).inflate(R.layout.tuesday_match,parent,false);        return new ViewHolder(v);    }    @Override    public void onBindViewHolder(@NonNull final ViewHolder holder, final int position) {        holder.title.setText(mode.get(position).getTitle());        holder.icons.setImageResource(mode.get(position).getImages());        holder.team.setText(mode.get(position).getTeam());        holder.images.setImageResource(mode.get(position).getIcon_team());        holder.soccer.setText(mode.get(position).getSoccer());        holder.pics.setImageResource(mode.get(position).getImage_team());        holder.teams.setText(mode.get(position).getVs_team());        holder.txtOption.setOnClickListener(new View.OnClickListener() {            @Override            public void onClick(View v) {                PopupMenu popupMenu = new PopupMenu(mcontext,holder.txtOption);                popupMenu.inflate(R.menu.option);                popupMenu.setOnMenuItemClickListener(new PopupMenu.OnMenuItemClickListener() {                    /// AlertDialog                    @Override                    public boolean onMenuItemClick(MenuItem item) {                        switch (item.getItemId()){                            case R.id.table:                                Toast.makeText(mcontext,"clicked",Toast.LENGTH_SHORT).show();                                break;                            case R.id.notif:                                //Toast.makeText(mcontext,"show",Toast.LENGTH_SHORT).show();                                AlertDialog.Builder builder = new AlertDialog.Builder(mcontext);                                String [] ColorsArray = new String[]{"Started","Full Time","Goals","Red Card","Missed Penalty"};                                final boolean [] CheckedColorArray = new boolean[]{                                        true,                                        false,                                        true,                                        false,                                        false                                };                                final List<String> colorList = Arrays.asList(ColorsArray);                                builder.setTitle("Set Alerts");                                builder.setMultiChoiceItems(ColorsArray, CheckedColorArray, new DialogInterface.OnMultiChoiceClickListener() {                                    @Override                                    public void onClick(DialogInterface dialog, int which, boolean isChecked) {                                        CheckedColorArray[which] = isChecked;                                        String currentItem = colorList.get(which);                                        Toast.makeText(mcontext,currentItem+""+ isChecked,Toast.LENGTH_SHORT).show();                                    }                                });                                builder.setPositiveButton("Ok", new DialogInterface.OnClickListener() {                                    @Override                                    public void onClick(DialogInterface dialog, int which) {                                    }                                });                                builder.setNegativeButton("Cancel", new DialogInterface.OnClickListener() {                                    @Override                                    public void onClick(DialogInterface dialog, int which) {                                    }                                });                                AlertDialog dialog = builder.create();                                dialog.show();                                break;                            default:                                break;                        }                        return false;                    }                });                popupMenu.show();            }        });    }    @Override    public int getItemCount() {        return mode.size();    }    public class ViewHolder extends RecyclerView.ViewHolder implements View.OnClickListener{        Context mcontext;        ImageView icons;        TextView title;        TextView teams,teams2,teams3;        ImageView images,images2,images3;        TextView soccer,soccer2,soccer3;        ImageView pics,pic2,pic3;        TextView team,team2,team3;        TextView txtOption;        public ViewHolder(@NonNull View itemView) {            super(itemView);            this.icons = itemView.findViewById(R.id.icons);            this.title =  itemView.findViewById(R.id.title_league);            this.team = itemView.findViewById(R.id.team);            this.images = itemView.findViewById(R.id.img_team);            this.soccer =  itemView.findViewById(R.id.soccers);            this.pics =  itemView.findViewById(R.id.img_team2);            this.teams =  itemView.findViewById(R.id.team2);            txtOption = itemView.findViewById(R.id.txt_option);            itemView.setClickable(true);            itemView.setOnClickListener(this);        }        @Override        public void onClick(View v) {            final Intent intent;            switch (getAdapterPosition()){                case 0:                    intent = new Intent(mcontext, News_Recycler.class);                    break;                case 1:                    intent = new Intent(mcontext, friday_cardview.class);                    break;                default:                    intent = new Intent(mcontext, Uni.class);                    break;            }            mcontext.startActivity(intent);        }    }}